<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <!--
            Las pruebas corren sobre MySQL, no sobre SQLite en memoria.

            SQLite es mas rapido, pero la plataforma usa funciones que solo
            existen en MySQL: REGEXP para buscar siglas de dos letras en los
            articulos, FIELD para ordenar el listado de tickets por prioridad,
            y DATE_FORMAT en el panel de administracion. Sobre SQLite esas
            pantallas no se pueden probar, y son de las mas usadas.

            Probar sobre el mismo motor que corre en produccion evita ademas
            una prueba que pasa aqui y falla alla.

            OJO: conecta_testing es una base APARTE de conecta. Las pruebas la
            vacian entera en cada corrida. Nunca apuntar esto a la base real.
        -->
        <env name="DB_CONNECTION" value="mysql"/>
        <env name="DB_DATABASE" value="conecta_testing"/>
        <env name="DB_URL" value=""/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
